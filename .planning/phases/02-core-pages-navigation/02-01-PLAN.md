---
phase: 02-core-pages-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Navigation.astro
  - src/layouts/BaseLayout.astro
autonomous: true
requirements: [NAV-01, NAV-03, DSGN-02]

must_haves:
  truths:
    - User can see navigation links to Archive, Experience, and Now pages
    - User can see contact email link on all pages
    - Navigation applies minimalist design system consistently
  artifacts:
    - path: src/components/Navigation.astro
      provides: Site navigation with email contact
      min_lines: 40
      exports: []
    - path: src/layouts/BaseLayout.astro
      provides: Shared page structure with navigation
      min_lines: 30
      contains: "<Navigation />"
  key_links:
    - from: src/layouts/BaseLayout.astro
      to: src/components/Navigation.astro
      via: component import
      pattern: "import Navigation from.*Navigation\\.astro"
    - from: src/components/Navigation.astro
      to: design tokens
      via: CSS variables
      pattern: "var\\(--"
---

<objective>
Create reusable navigation component and base layout that all pages will use.

Purpose: Establish consistent site structure and navigation patterns before building individual pages. Enables parallel page development in Wave 2.

Output: Navigation component with Archive/Experience/Now links + email contact, wrapped in BaseLayout that provides shared HTML structure.
</objective>

<execution_context>
@/Users/arthurpfalzgraf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arthurpfalzgraf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@src/styles/global.css
@src/styles/tokens/semantic.css
@src/styles/tokens/components.css
</context>

<tasks>

<task type="auto">
  <name>Create Navigation component</name>
  <files>src/components/Navigation.astro</files>
  <action>
Create Navigation.astro component with:
- Three navigation links: Archive (/archive), Experience (/experience), Now (/now)
- Email contact link: arthur@arthurpfz.com (visible, not hidden in footer)
- Horizontal layout using flexbox (desktop) with simple responsive stack on mobile
- Apply design tokens from semantic layer: --color-action-primary for links, --space-nav-* for spacing
- Use semantic HTML: &lt;nav&gt; element with &lt;ul&gt;&lt;li&gt; structure
- Active state styling: underline or color change for current page (using Astro.url.pathname)
- Minimal design matching nousresearch.com aesthetic: clean typography, ample whitespace, no decorative elements
- Hover states for links using --color-action-hover token

Do NOT use complex animations or transitions. Do NOT add logo or branding. Keep it text-only navigation.
  </action>
  <verify>
    <automated>npm run build && grep -q "arthur@arthurpfz.com" dist/archive/index.html</automated>
    <manual>Visual check: Navigate to each page, verify navigation links work and email is visible</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Navigation component exists with Archive/Experience/Now links and email contact. Links use semantic color tokens. Active page is visually distinguished.</done>
</task>

<task type="auto">
  <name>Create BaseLayout wrapper</name>
  <files>src/layouts/BaseLayout.astro</files>
  <action>
Create BaseLayout.astro layout component with:
- Accept props: title (string, required), description (string, optional, default: "Portfolio of Arthur Pfalzgraf")
- HTML structure: &lt;html lang="en"&gt;, &lt;head&gt; with meta tags, &lt;body&gt; with Navigation and &lt;slot /&gt;
- Import global.css for design tokens
- Meta tags: charset UTF-8, viewport for responsive, description, title
- Body uses semantic tokens: background-color: var(--color-background-primary), color: var(--color-foreground-primary)
- Main content area: &lt;main&gt; wrapper around &lt;slot /&gt; with max-width (--layout-max-width) and centered (margin: 0 auto)
- Navigation component imported and placed before &lt;main&gt;
- Minimal padding/margin using --space-layout-* tokens

TypeScript interface for props:
```typescript
interface Props {
  title: string;
  description?: string;
}
```

Do NOT add footer. Do NOT add sidebar. Keep it simple: nav + main content area.
  </action>
  <verify>
    <automated>npm run build && grep -q "&lt;Navigation" dist/archive/index.html</automated>
    <manual>Build passes, BaseLayout can be imported by pages, Navigation renders</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>BaseLayout exists, accepts title/description props, includes Navigation component, provides centered content area with max-width constraint.</done>
</task>

</tasks>

<verification>
Manual checks after plan completion:
1. Start dev server (npm run dev)
2. Visit http://localhost:4321/
3. Verify navigation component visible with all three page links
4. Verify email link (arthur@arthurpfz.com) is visible and clickable
5. Check that design tokens apply correctly (link colors, spacing)
6. Verify responsive behavior by resizing browser window
</verification>

<success_criteria>
- Navigation component exists with Archive, Experience, Now links
- Email contact (arthur@arthurpfz.com) is visible in navigation
- BaseLayout exists and includes Navigation component
- Design system applied: semantic color tokens for links, spacing tokens for layout
- Build succeeds without errors
- Navigation renders in generated HTML
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-pages-navigation/02-01-SUMMARY.md`
</output>
