---
phase: 03-responsive-accessible-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/global.css
  - src/components/Navigation.astro
  - src/layouts/BaseLayout.astro
  - src/pages/archive.astro
  - src/pages/experience.astro
  - src/pages/now.astro
autonomous: false
requirements: [DSGN-03]

must_haves:
  truths:
    - "User can navigate and read content comfortably on mobile devices (phone and tablet)"
    - "Touch targets are appropriately sized (minimum 44px) on mobile"
    - "Typography scales appropriately across viewport sizes"
    - "Layout adapts gracefully to small screens without horizontal scrolling"
  artifacts:
    - path: "src/styles/global.css"
      provides: "Responsive typography and spacing adjustments"
      contains: "@media"
    - path: "src/components/Navigation.astro"
      provides: "Mobile-optimized touch targets"
      min_lines: 90
    - path: "src/layouts/BaseLayout.astro"
      provides: "Viewport meta tag and responsive container"
      contains: "viewport"
  key_links:
    - from: "src/styles/global.css"
      to: "all pages"
      via: "responsive media queries"
      pattern: "@media.*max-width"
    - from: "src/layouts/BaseLayout.astro"
      to: "viewport rendering"
      via: "meta viewport tag"
      pattern: "viewport.*width=device-width"
---

<objective>
Ensure portfolio site provides excellent user experience on mobile devices (phones and tablets) with proper touch target sizing, responsive typography, and adaptive layouts.

Purpose: Mobile devices represent significant portion of web traffic. Site must be readable and usable without pinch-zoom or horizontal scrolling.

Output: Fully responsive site that adapts gracefully from 320px mobile to desktop widths.
</objective>

<execution_context>
@/Users/arthurpfalzgraf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arthurpfalzgraf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Design token system
@.planning/phases/01-foundation-design-system/design-tokens.json
@src/styles/tokens/semantic.css

# Existing components
@src/components/Navigation.astro
@src/layouts/BaseLayout.astro
@src/pages/archive.astro
@src/pages/experience.astro
@src/pages/now.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement responsive typography and spacing system</name>
  <files>src/styles/global.css</files>
  <action>
Add responsive typography adjustments to global.css using fluid typography approach:

1. Add clamp() functions for heading sizes that scale smoothly between mobile (320px) and desktop (1440px)
   - H1: clamp(2rem, 5vw + 1rem, 3.75rem) - scales from 32px to 60px
   - H2: clamp(1.75rem, 4vw + 0.75rem, 3rem) - scales from 28px to 48px
   - H3: clamp(1.5rem, 3vw + 0.5rem, 2.25rem) - scales from 24px to 36px
   - H4-H6: Smaller incremental scaling

2. Add mobile-specific spacing adjustments at 640px breakpoint:
   - Reduce section spacing (--space-section-md, --space-section-lg) by 25% on mobile
   - Reduce content spacing slightly for tighter mobile layouts

3. Ensure body text remains at 16px minimum (never scale below) for readability

4. Add max-width constraint on body text (65ch) for optimal reading line length

Reference existing design tokens from semantic.css. Do NOT modify token files - add responsive overrides in global.css only.
  </action>
  <verify>
    <automated>npm run build</automated>
    <manual>Inspect dist/archive/index.html to verify clamp() functions present in generated CSS and breakpoints correctly applied</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>Build succeeds, typography scales smoothly across viewport widths, generated CSS contains clamp() and @media rules</done>
</task>

<task type="auto">
  <name>Task 2: Optimize touch targets for mobile navigation</name>
  <files>src/components/Navigation.astro</files>
  <action>
Update Navigation component to ensure WCAG 2.1 Level AA touch target compliance (minimum 44x44px):

1. Increase nav-link padding to ensure 44px minimum height on mobile:
   - Add padding: 0.75rem 1rem (12px vertical = 12+16+12 = 40px, close enough with line-height)
   - Adjust with larger vertical padding at mobile breakpoint if needed

2. Increase gap between nav items on mobile to prevent mis-taps:
   - Mobile: gap of var(--space-content-lg) or larger (24px minimum)

3. Ensure email link has same touch target size

4. Test that active state underline doesn't reduce touch area

Keep existing responsive behavior (horizontal on desktop, vertical stack on mobile at 640px). Only adjust sizing, not layout structure.
  </action>
  <verify>
    <automated>npm run build && grep -o "padding:.*rem.*rem" dist/archive/index.html | head -5</automated>
    <manual>Check that navigation links have comfortable tap targets on mobile viewport</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>Navigation links have 44px minimum touch targets on mobile, adequate spacing between items prevents mis-taps</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Responsive mobile design across all pages with fluid typography and mobile-optimized touch targets</what-built>
  <how-to-verify>
1. Start dev server: `npm run dev`
2. Open browser to http://localhost:4321/archive
3. Open browser DevTools and switch to responsive/mobile view
4. Test multiple viewport sizes:
   - Mobile: 375x667 (iPhone SE)
   - Tablet: 768x1024 (iPad)
   - Desktop: 1440x900

5. Verify on each viewport size:
   - No horizontal scrolling required
   - All text is readable without zooming
   - Navigation links are easy to tap (not too small, not too close together)
   - Typography sizes feel appropriate (not too large on mobile, not too small on desktop)
   - Content spacing feels balanced (not cramped on mobile, not too spread on desktop)

6. Test all three pages: /archive, /experience, /now

7. Check that heading sizes scale smoothly as you resize viewport (not sudden jumps)

Expected: Site looks good and functions well on all tested viewport sizes. Touch targets are comfortable on mobile. No accessibility issues visible in DevTools.
  </how-to-verify>
  <resume-signal>Type "approved" if responsive design works well across viewports, or describe issues found</resume-signal>
</task>

</tasks>

<verification>
Overall phase verification:

1. **Mobile viewport test**: Site renders correctly on 375px mobile width without horizontal scroll
2. **Touch target test**: All interactive elements meet 44px minimum on mobile
3. **Typography test**: Text scales appropriately and remains readable across all viewport sizes
4. **Build test**: `npm run build` succeeds with no errors or warnings
5. **Visual regression**: Site maintains design consistency at all breakpoints
</verification>

<success_criteria>
- [x] Typography scales smoothly using fluid type (clamp functions)
- [x] Mobile spacing optimizations applied (reduced section spacing)
- [x] Touch targets meet WCAG 2.1 AA minimum (44x44px)
- [x] No horizontal scrolling on any viewport size (320px+)
- [x] All pages tested on mobile, tablet, desktop viewports
- [x] Navigation remains usable and comfortable on all screen sizes
- [x] Build succeeds with responsive CSS properly generated
</success_criteria>

<output>
After completion, create `.planning/phases/03-responsive-accessible-design/03-01-SUMMARY.md`
</output>
