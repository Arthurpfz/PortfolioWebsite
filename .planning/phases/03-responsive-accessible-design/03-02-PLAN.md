---
phase: 03-responsive-accessible-design
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/Navigation.astro
  - src/layouts/BaseLayout.astro
  - src/pages/archive.astro
  - src/pages/experience.astro
  - src/pages/now.astro
autonomous: true
requirements: [DSGN-07]

must_haves:
  truths:
    - "User can navigate entire site using keyboard only (no mouse required)"
    - "Screen reader announces page structure correctly (headings, navigation, content regions)"
    - "All interactive elements have appropriate ARIA labels"
    - "Focus indicators are clearly visible on all interactive elements"
  artifacts:
    - path: "src/components/Navigation.astro"
      provides: "Keyboard-accessible navigation with focus styles"
      contains: "focus-visible"
    - path: "src/layouts/BaseLayout.astro"
      provides: "Semantic landmarks (main, nav)"
      contains: "role.*main"
    - path: "src/pages/archive.astro"
      provides: "Semantic article structure with proper heading hierarchy"
      contains: "aria-label"
  key_links:
    - from: "Navigation links"
      to: "keyboard focus"
      via: ":focus-visible styles"
      pattern: "focus-visible.*outline"
    - from: "BaseLayout landmarks"
      to: "screen reader navigation"
      via: "ARIA landmark roles"
      pattern: "role=.(navigation|main)"
    - from: "Page headings"
      to: "document outline"
      via: "proper h1-h6 hierarchy"
      pattern: "<h[1-6]"
---

<objective>
Ensure portfolio site meets WCAG 2.1 Level AA accessibility standards with full keyboard navigation support, screen reader compatibility, and semantic HTML structure.

Purpose: Accessibility is a baseline requirement, not a nice-to-have. Site must be usable by people using assistive technologies, keyboard-only navigation, or screen readers.

Output: Fully accessible site that passes automated accessibility checks and supports keyboard/screen reader navigation.
</objective>

<execution_context>
@/Users/arthurpfalzgraf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arthurpfalzgraf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Existing components (need accessibility enhancements)
@src/components/Navigation.astro
@src/layouts/BaseLayout.astro
@src/pages/archive.astro
@src/pages/experience.astro
@src/pages/now.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement keyboard navigation and focus management</name>
  <files>src/components/Navigation.astro</files>
  <action>
Enhance Navigation component with keyboard accessibility:

1. Add visible focus indicators for keyboard users using :focus-visible (NOT :focus to avoid showing on mouse clicks):
   ```css
   .nav-link:focus-visible {
     outline: 2px solid var(--color-action-primary);
     outline-offset: 4px;
     border-radius: var(--radius-sm);
   }
   ```

2. Ensure focus order is logical (Archive → Experience → Now → Email in DOM order)

3. Add skip-to-content link for keyboard users (hidden visually but available to screen readers and keyboard):
   - Link that jumps to main content, bypassing navigation
   - Positioned before nav, only visible on keyboard focus
   - Use class `.skip-link` with position: absolute, clip pattern for screen-reader-only visibility

4. Verify tab order works correctly (no tabindex needed if DOM order is correct)

Do NOT use tabindex unless absolutely necessary. Native DOM order should handle tab flow.
  </action>
  <verify>
    <automated>npm run build && grep -c "focus-visible" dist/archive/index.html</automated>
    <manual>Use Tab key to navigate through links, verify visible focus indicator appears</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>All navigation links have visible focus indicators, skip-to-content link exists, keyboard tab order is logical</done>
</task>

<task type="auto">
  <name>Task 2: Add ARIA landmarks and semantic structure</name>
  <files>src/layouts/BaseLayout.astro, src/components/Navigation.astro</files>
  <action>
Add ARIA landmarks and improve semantic HTML structure:

1. In BaseLayout.astro:
   - Ensure `<main>` element has `role="main"` for older screen readers
   - Add `aria-label="Site navigation"` to Navigation component usage
   - Add `lang="en"` to html element (already exists, verify)

2. In Navigation.astro:
   - Add `role="navigation"` to `<nav>` element for older screen reader support
   - Add `aria-label="Site navigation"` to `<nav>` element
   - Keep existing `aria-current="page"` on active link (already implemented)

3. Verify skip-link targets main content:
   - Add `id="main-content"` to `<main>` in BaseLayout
   - Skip-link should have `href="#main-content"`

These ARIA additions provide redundancy for older assistive tech while modern browsers use semantic HTML.
  </action>
  <verify>
    <automated>npm run build && grep -E "(role=.main|aria-label)" dist/archive/index.html | head -5</automated>
    <manual>Verify ARIA attributes present in generated HTML</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>Semantic landmarks defined (main, navigation), ARIA labels added for screen reader context, skip-link targets main content</done>
</task>

<task type="auto">
  <name>Task 3: Ensure proper heading hierarchy and page structure</name>
  <files>src/pages/archive.astro, src/pages/experience.astro, src/pages/now.astro</files>
  <action>
Audit and fix heading hierarchy across all pages to ensure proper document outline:

1. Verify each page has exactly one H1 (page title)
2. Verify heading levels don't skip (H1 → H2 → H3, never H1 → H3)
3. Add aria-label to sections for screen reader context where helpful:

For archive.astro:
- H1: "Arthur's PM Homework" ✓ (already correct)
- H2: "Articles" ✓ (already correct)
- Add `aria-label="Article archive"` to `<section class="articles-section">`

For experience.astro:
- H1: "Experience" ✓ (already correct)
- H3: Company names ✓ (already correct)
- Add `aria-label="Work history"` to `<section class="work-history">`

For now.astro:
- H1: "Now" ✓ (already correct)
- Add `aria-label="Current thoughts and projects"` to `<section class="thoughts-list">`

4. Ensure article elements have semantic structure (existing `<article class="role">` in experience.astro is good)

5. Add visually-hidden text for context where needed (e.g., "Link opens in new tab" for external links)

Do NOT change visual design. Only add semantic HTML attributes for assistive technology.
  </action>
  <verify>
    <automated>npm run build && grep -c "<h1" dist/archive/index.html && grep -c "<h1" dist/experience/index.html && grep -c "<h1" dist/now/index.html</automated>
    <manual>Each page should show exactly 1 h1 tag. Verify heading hierarchy is correct (no skipped levels)</manual>
    <sampling_rate>run after task commits</sampling_rate>
  </verify>
  <done>Each page has single H1, heading hierarchy is proper (no skipped levels), sections have aria-labels for screen reader context</done>
</task>

</tasks>

<verification>
Overall accessibility verification:

1. **Automated accessibility audit**: Run Lighthouse accessibility audit on all pages (target: 95+ score)
2. **Keyboard navigation**: Tab through entire site using only keyboard, verify all interactive elements reachable and focus visible
3. **Screen reader test**: Use VoiceOver (macOS) or NVDA (Windows) to navigate, verify page structure announced correctly
4. **Heading hierarchy**: Verify document outline shows logical heading structure with no skipped levels
5. **ARIA validation**: Verify ARIA attributes are present and valid in generated HTML
6. **Semantic HTML**: Verify proper use of nav, main, article, section elements
</verification>

<success_criteria>
- [x] All interactive elements keyboard accessible with visible focus indicators
- [x] Skip-to-content link implemented for keyboard users
- [x] ARIA landmarks (navigation, main) defined with labels
- [x] Heading hierarchy correct on all pages (single H1, no skipped levels)
- [x] Sections have contextual aria-labels for screen readers
- [x] External links indicate they open in new tab
- [x] Build succeeds, accessibility attributes present in generated HTML
- [x] Lighthouse accessibility score 95+ on all pages
</success_criteria>

<output>
After completion, create `.planning/phases/03-responsive-accessible-design/03-02-SUMMARY.md`
</output>
