---
phase: 03-responsive-accessible-design
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [src/pages/experience.astro]
autonomous: true
gap_closure: true
requirements: [DSGN-07]

must_haves:
  truths:
    - "Screen reader announces page structure correctly on Experience page"
    - "Heading hierarchy follows WCAG 2.1 Success Criterion 1.3.1 (no skipped levels)"
    - "Screen reader users can navigate by headings without confusion"
  artifacts:
    - path: "src/pages/experience.astro"
      provides: "Proper heading hierarchy (H1 → H2 → H3 or H1 → H2 only)"
      min_lines: 135
      contains: "<h2>"
  key_links:
    - from: "experience.astro H1"
      to: "section heading or company headings"
      via: "proper heading level progression"
      pattern: "<h1>.*</h1>[\\s\\S]*<h2>"
---

<objective>
Fix heading hierarchy violation on Experience page to achieve full WCAG 2.1 Level AA compliance.

Purpose: Screen readers rely on proper heading hierarchy to create document outlines. Skipping from H1 to H3 confuses screen reader users navigating by headings. This gap prevents full accessibility compliance (DSGN-07).

Output: Experience page with valid heading hierarchy, closing the final gap for Phase 3 verification.
</objective>

<execution_context>
@/Users/arthurpfalzgraf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arthurpfalzgraf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-responsive-accessible-design/03-VERIFICATION.md
@.planning/phases/03-responsive-accessible-design/03-02-SUMMARY.md
@src/pages/experience.astro
</context>

<tasks>

<task type="auto">
  <name>Fix heading hierarchy on Experience page</name>
  <files>src/pages/experience.astro</files>
  <action>
Fix the heading hierarchy violation identified in VERIFICATION.md. Current structure has H1 → H3 (skips H2).

**Two valid fix options:**

**Option 1 (recommended):** Change company H3 headings to H2
- Lines 20, 26, 32: Change `<h3>Company Name</h3>` to `<h2>Company Name</h2>`
- Update corresponding CSS selector from `h3` to `h2` (line 85)
- Semantically correct: each company is a top-level section within the work history

**Option 2:** Add H2 section heading before company listings
- Insert `<h2>Work History</h2>` after line 16 (before the example structure comment)
- Keep existing H3 company headings
- More hierarchical but adds visual element that may not match design intent

**Choose Option 1** (company headings as H2) for cleaner implementation and semantic correctness. Each role/company is a primary section of the work history, not a subsection.

Rationale per WCAG 2.1 SC 1.3.1: Heading levels must not skip. H1 (Experience) → H2 (Company names) is valid and semantically appropriate for this content structure.
  </action>
  <verify>
    <automated>grep -E "^\s*<h2>" src/pages/experience.astro && ! grep -E "^\s*<h3>" src/pages/experience.astro | head -5</automated>
    <manual>Verify experience.astro now has H1 → H2 progression with no H3 tags</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>
- experience.astro heading structure is H1 → H2 (no skipped levels)
- No H3 headings remain on the page
- CSS selector updated to match H2 instead of H3
- Grep confirms H2 tags present and H3 tags absent in company listings
  </done>
</task>

</tasks>

<verification>
After gap closure:

1. **Heading hierarchy automated check:**
```bash
# Verify H1 exists
grep -c "<h1>" src/pages/experience.astro  # Should be 1

# Verify H2 exists (company headings)
grep -c "<h2>" src/pages/experience.astro  # Should be 3+ (example companies)

# Verify no H3 exists (skipped level eliminated)
grep -c "<h3>" src/pages/experience.astro  # Should be 0
```

2. **Visual verification (manual):**
- Run `npm run dev`
- Visit http://localhost:4321/experience
- Verify company headings still look appropriate (H2 styling should be similar to previous H3)
- Typography hierarchy should feel natural (H1 page title → H2 company names)

3. **Screen reader verification (manual - recommended but optional):**
- Use VoiceOver (macOS: Cmd+F5) or NVDA (Windows)
- Navigate to /experience page
- Press H key to jump between headings
- Should announce: "Heading level 1: Experience" → "Heading level 2: Company Name" (no skipped levels)
</verification>

<success_criteria>
- WCAG 2.1 Success Criterion 1.3.1 satisfied (proper heading hierarchy)
- Screen readers can navigate Experience page heading structure without confusion
- DSGN-07 requirement fully satisfied (was partially satisfied, now complete)
- Phase 3 VERIFICATION.md gap closed
- experience.astro contains H2 company headings, no H3 headings
</success_criteria>

<output>
After completion, create `.planning/phases/03-responsive-accessible-design/03-03-SUMMARY.md`
</output>
