---
phase: 04-content-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/config.ts
  - src/content/articles/designing-for-behavior-change.md
  - src/content/articles/dont-make-me-think.md
  - src/content/articles/good-bad-strategy.md
  - src/content/articles/outcomes-over-outputs.md
  - src/content/articles/black-box-thinking.md
  - src/content/articles/brainfluence.md
  - src/content/articles/cracked-it.md
  - src/content/articles/hooked.md
  - src/content/articles/inspired-marty-cagan.md
  - src/content/articles/predictably-irrational.md
  - src/content/articles/speak-without-freaking.md
  - src/content/articles/lean-product-playbook.md
  - src/content/articles/user-stories.md
  - src/pages/archive.astro
autonomous: true
requirements: [PAGE-02]

must_haves:
  truths:
    - "User can view all previously published articles on the Archive page"
    - "Article titles, dates, and descriptions are visible"
    - "Articles are sorted by publication date (newest first)"
  artifacts:
    - path: "src/content/config.ts"
      provides: "Content collection schema for articles"
      exports: ["collections"]
    - path: "src/content/articles/*.md"
      provides: "13 migrated articles with frontmatter"
      min_lines: 1300
    - path: "src/pages/archive.astro"
      provides: "Archive page with dynamic article list"
      contains: "getCollection"
  key_links:
    - from: "src/pages/archive.astro"
      to: "src/content/articles/"
      via: "getCollection('articles')"
      pattern: "getCollection.*articles"
    - from: "src/content/config.ts"
      to: "zod schema"
      via: "defineCollection"
      pattern: "defineCollection"
---

<objective>
Migrate 13 Hugo articles to Astro content collections and display them on the Archive page.

Purpose: Make existing PM homework articles accessible in the new Astro site with preserved metadata (titles, dates, descriptions, tags).

Output: Working Archive page showing all migrated articles in reverse chronological order.
</objective>

<execution_context>
@/Users/arthurpfalzgraf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arthurpfalzgraf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@src/pages/archive.astro
@content/post/Designing_for_behavior_change.md
@content/post/Outcomes_over_outputs.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Astro content collection for articles</name>
  <files>src/content/config.ts</files>
  <action>
Create Astro content collection configuration at `src/content/config.ts`:

1. Import `defineCollection` and `z` from `astro:content`
2. Define `articles` collection with schema:
   - `title`: z.string()
   - `date`: z.coerce.date() (converts string dates to Date objects)
   - `description`: z.string()
   - `tags`: z.array(z.string()).optional()
   - `categories`: z.array(z.string()).optional()
   - `draft`: z.boolean().default(false)
3. Export collections object with `articles` collection
4. Use `glob()` loader (not `file()`) as Astro 6 default for markdown collections

Pattern follows Astro 6 content collections API (https://docs.astro.build/en/guides/content-collections/).
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -q "content collections" && echo "PASS: Content collection recognized" || echo "FAIL: Content collection not recognized"</automated>
    <manual>Check that src/content/config.ts exists and exports collections</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Content collection schema defined, Astro recognizes articles collection during build</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Hugo articles to Astro content collection</name>
  <files>
    src/content/articles/designing-for-behavior-change.md
    src/content/articles/dont-make-me-think.md
    src/content/articles/good-bad-strategy.md
    src/content/articles/outcomes-over-outputs.md
    src/content/articles/black-box-thinking.md
    src/content/articles/brainfluence.md
    src/content/articles/cracked-it.md
    src/content/articles/hooked.md
    src/content/articles/inspired-marty-cagan.md
    src/content/articles/predictably-irrational.md
    src/content/articles/speak-without-freaking.md
    src/content/articles/lean-product-playbook.md
    src/content/articles/user-stories.md
  </files>
  <action>
Migrate 13 articles from `content/post/*.md` to `src/content/articles/*.md`:

For each file in content/post/:
1. Create corresponding file in src/content/articles/ with kebab-case filename:
   - `Designing_for_behavior_change.md` → `designing-for-behavior-change.md`
   - `DontMakeMeThink.md` → `dont-make-me-think.md`
   - `GoodBadStrategy.md` → `good-bad-strategy.md`
   - (continue for all 13 files)

2. Copy frontmatter and content exactly:
   - Preserve title, date, description, tags, categories, draft fields
   - Keep full markdown content including <!--more--> and all text
   - DO NOT modify image paths yet (Task 3 in Plan 04-02 will handle)

3. Verify all 13 files migrated (count matches)

File mapping:
- Designing_for_behavior_change.md → designing-for-behavior-change.md
- DontMakeMeThink.md → dont-make-me-think.md
- GoodBadStrategy.md → good-bad-strategy.md
- Outcomes_over_outputs.md → outcomes-over-outputs.md
- black_box_thinking.md → black-box-thinking.md
- brainfluence.md → brainfluence.md
- crackedIt.md → cracked-it.md
- hooked.md → hooked.md
- inspired_marty_cagan.md → inspired-marty-cagan.md
- my-first-post.md → (skip - appears to be template/test)
- predictablyIrrational.md → predictably-irrational.md
- speakWitoutFreaking.md → speak-without-freaking.md
- sum_lean_product_playbook.md → lean-product-playbook.md
- sum_user_stories.md → user-stories.md

Total: 13 articles (skip my-first-post.md as it appears to be a Hugo test file).
  </action>
  <verify>
    <automated>[ $(ls src/content/articles/*.md 2>/dev/null | wc -l) -eq 13 ] && echo "PASS: All 13 articles migrated" || echo "FAIL: Article count mismatch"</automated>
    <manual>Verify frontmatter preserved for sample articles</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>13 articles exist in src/content/articles/ with preserved frontmatter and content</done>
</task>

<task type="auto">
  <name>Task 3: Update Archive page to display articles</name>
  <files>src/pages/archive.astro</files>
  <action>
Update `src/pages/archive.astro` to load and display articles from content collection:

1. Import `getCollection` from `astro:content`
2. Fetch articles in frontmatter:
   ```typescript
   const articles = await getCollection('articles');
   const publishedArticles = articles
     .filter(article => !article.data.draft)
     .sort((a, b) => b.data.date.getTime() - a.data.date.getTime());
   ```
3. Replace placeholder `<p>Articles will appear here...</p>` with article list
4. For each article, display:
   - Title as link to `/archive/{slug}`
   - Publication date (formatted as "YYYY-MM-DD" or "Month DD, YYYY")
   - Description
   - Tags (if present)
5. Use existing `.article-list` styles, add minimal new styles for:
   - `.article-item` (margin-bottom between articles)
   - `.article-meta` (date + tags formatting)
   - `.article-title` (link styling using --color-action-primary)

Keep minimalist aesthetic - no cards, borders, or heavy decoration. Simple list with clear typography hierarchy.

Note: Individual article pages (/archive/{slug}) will be created in Plan 04-03 (deferred) if needed. For now, links can point to placeholder or be unstyled `<span>` elements instead of `<a>` tags.
  </action>
  <verify>
    <automated>npm run build && grep -q "getCollection" dist/archive/index.html && echo "PASS: Archive page renders with article collection" || echo "FAIL: Archive page not rendering articles"</automated>
    <manual>Visit localhost:4321/archive and verify all 13 articles appear with titles, dates, descriptions</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Archive page displays 13 articles with titles, dates, descriptions in reverse chronological order</done>
</task>

</tasks>

<verification>
Run dev server and verify:
1. Visit /archive - should show 13 articles
2. Articles sorted newest first
3. Each article shows title, date, description
4. Build succeeds with no content collection errors
</verification>

<success_criteria>
- [x] Content collection schema defined for articles
- [x] All 13 Hugo articles migrated to src/content/articles/
- [x] Article metadata preserved (title, date, description, tags, categories)
- [x] Archive page displays all articles dynamically from content collection
- [x] Articles sorted by date (newest first)
- [x] Build succeeds, /archive accessible
- [x] Minimalist design maintained
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-migration/04-01-SUMMARY.md`
</output>
