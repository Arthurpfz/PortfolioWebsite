---
phase: 04-content-migration
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - src/pages/archive/[slug].astro
  - src/pages/archive.astro
  - public/_redirects
autonomous: true
requirements: [PAGE-02]
gap_closure: true

must_haves:
  truths:
    - "User can click on article titles from Archive page to view full article"
    - "User can read individual articles at /archive/{slug} URLs"
    - "Old Hugo URLs (/post/{slug}/) redirect to new /archive/{slug} URLs"
    - "Article content renders with proper typography and images"
  artifacts:
    - path: "src/pages/archive/[slug].astro"
      provides: "Individual article page with content rendering"
      exports: ["getStaticPaths"]
      min_lines: 80
    - path: "public/_redirects"
      provides: "URL redirects for old Hugo paths"
      contains: "/post/* /archive/:splat 301"
  key_links:
    - from: "src/pages/archive.astro"
      to: "src/pages/archive/[slug].astro"
      via: "anchor link on article title"
      pattern: "<a href=.*/archive/"
    - from: "src/pages/archive/[slug].astro"
      to: "src/content/articles/"
      via: "getCollection and render"
      pattern: "getCollection.*articles.*render"
---

<objective>
Close verification gap by creating individual article pages and URL redirects

Purpose: Enable users to read full articles and preserve old Hugo URLs
Output: Dynamic article pages at /archive/{slug} and redirect configuration
</objective>

<execution_context>
@/Users/arthurpfalzgraf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arthurpfalzgraf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-migration/04-01-SUMMARY.md
@.planning/phases/04-content-migration/04-02-SUMMARY.md
@.planning/phases/04-content-migration/04-VERIFICATION.md

@src/content.config.ts
@src/pages/archive.astro
@src/content/articles/hooked.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create dynamic article page</name>
  <files>src/pages/archive/[slug].astro</files>
  <action>
Create dynamic route page at src/pages/archive/[slug].astro that:

1. Implements getStaticPaths() to generate routes for all articles:
   - Use getCollection('articles') to load all articles
   - Filter out drafts
   - Return array of { params: { slug }, props: { entry } } for each article
   - Extract slug from entry.id (filename without .md extension)

2. Receives article entry via Astro.props
3. Renders article content using entry.render():
   - Call const { Content } = await entry.render()
   - Render <Content /> component in article body
4. Wraps in BaseLayout with article title and description
5. Displays article metadata (title, date, tags) above content
6. Uses consistent typography variables from design tokens
7. Applies proper article content styling:
   - Prose styling for markdown content (headings, paragraphs, lists, links)
   - Image styling to constrain max-width and center
   - Code block styling if present
   - Proper spacing between elements

Reference existing archive.astro for design token usage patterns. Use Astro 6 content collections API (entry.render() returns Content component, not html string).
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -E "page.*generated|error" && ls -la dist/archive/hooked/index.html</automated>
    <manual>Visit localhost:4321/archive/hooked in browser - article displays with proper formatting and images</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Build generates static HTML for all 13 articles in dist/archive/{slug}/index.html, article content renders with typography styling, images display correctly</done>
</task>

<task type="auto">
  <name>Task 2: Make article titles clickable on Archive page</name>
  <files>src/pages/archive.astro</files>
  <action>
Update archive.astro to make article titles clickable:

1. Wrap article.data.title in anchor tag linking to /archive/{article.id}
2. Extract slug from article.id (remove .md extension if present)
3. Apply link styling:
   - Use color: var(--color-foreground-primary) for consistency
   - Add :hover state with underline
   - Remove default link underline
4. Ensure link is accessible (proper color contrast maintained)

Pattern: `<a href={`/archive/${article.id}`}>{article.data.title}</a>`
  </action>
  <verify>
    <automated>grep -n 'href.*archive' src/pages/archive.astro</automated>
    <manual>Visit localhost:4321/archive - click article title - navigates to /archive/{slug}</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Archive page article titles are clickable links, clicking navigates to individual article page, link styling matches minimalist design</done>
</task>

<task type="auto">
  <name>Task 3: Add URL redirects for old Hugo paths</name>
  <files>public/_redirects</files>
  <action>
Create public/_redirects file for Cloudflare Pages redirect rules:

1. Redirect old Hugo article URLs to new Archive URLs:
   - /post/* → /archive/:splat (301 permanent redirect)
   - Use :splat to capture slug from old URL
2. Add trailing slash handling:
   - /post/*/ → /archive/:splat (301) - handles URLs with trailing slash
3. Add comment explaining Hugo migration

Format:
```
# Hugo to Astro migration redirects
/post/*/ /archive/:splat 301
/post/* /archive/:splat 301
```

Cloudflare Pages reads _redirects automatically during deployment. 301 status indicates permanent redirect for SEO.
  </action>
  <verify>
    <automated>cat public/_redirects | grep -c "301"</automated>
    <manual>Deploy to Cloudflare Pages and test /post/hooked/ redirects to /archive/hooked (deferred until Phase 5 deployment)</manual>
    <sampling_rate>run after this task commits</sampling_rate>
  </verify>
  <done>_redirects file exists with Hugo migration rules, local build includes file in dist/</done>
</task>

</tasks>

<verification>
## Automated Checks

```bash
# Verify dynamic route generates all article pages
npm run build 2>&1 | grep "page.*generated" | wc -l  # Should show 13+ pages

# Verify specific article pages exist in build
ls dist/archive/hooked/index.html
ls dist/archive/lean-product-playbook/index.html
ls dist/archive/outcomes-over-outputs/index.html

# Verify archive page has clickable links
grep -c 'href.*archive' src/pages/archive.astro  # Should be 13+ (one per article)

# Verify redirects file exists
cat public/_redirects | grep "301"
```

## Manual Verification

1. **Individual Article Pages:**
   - Visit localhost:4321/archive/hooked
   - Article displays with title, date, tags, full content
   - Images render correctly (hooked1.png, hooked2.png, hooked3.png visible)
   - Typography uses design tokens (proper heading hierarchy, body text spacing)

2. **Archive Navigation:**
   - Visit localhost:4321/archive
   - Click on "Hooked, N.Eyal" title
   - Navigates to /archive/hooked
   - Click browser back button
   - Returns to archive list

3. **Build Output:**
   - Run npm run build
   - Check dist/archive/ directory contains 13 subdirectories (one per article)
   - Each subdirectory contains index.html file
</verification>

<success_criteria>
- Individual article pages exist at /archive/{slug} for all 13 articles
- Archive page article titles are clickable and navigate to article pages
- Article content renders with proper markdown formatting (headings, paragraphs, lists, images)
- Design tokens applied consistently (typography, spacing, colors)
- Old Hugo URLs (/post/{slug}/) configured to redirect to /archive/{slug}
- Build succeeds and generates static HTML for all article pages
- Gap from VERIFICATION.md is closed: "Old article URLs redirect properly to new Archive structure"
</success_criteria>

<output>
After completion, create `.planning/phases/04-content-migration/04-03-SUMMARY.md`
</output>
