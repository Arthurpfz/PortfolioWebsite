---
phase: 05-performance-launch
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - wrangler.toml
  - package.json
autonomous: false
requirements: [DSGN-04]

user_setup:
  - service: cloudflare-pages
    why: "Static site hosting with CDN and edge network"
    env_vars: []
    account_setup:
      - task: "Create Cloudflare account"
        url: "https://dash.cloudflare.com/sign-up"
        condition: "if not already registered"
      - task: "Add arthurpfz.com domain to Cloudflare"
        url: "https://dash.cloudflare.com"
        condition: "if domain not already on Cloudflare"
    dashboard_config:
      - task: "Create new Pages project"
        location: "Workers & Pages > Create application > Pages > Connect to Git"
        note: "Claude will attempt CLI deployment first"

must_haves:
  truths:
    - Site is live at arthurpfz.com
    - HTTPS/SSL is configured and working
    - All pages are accessible via production URL
    - Old Hugo URLs redirect to new Astro URLs
  artifacts:
    - path: "wrangler.toml"
      provides: "Cloudflare Pages configuration"
      contains: "name ="
    - path: "package.json"
      provides: "Deploy script"
      contains: "wrangler pages deploy"
  key_links:
    - from: "wrangler.toml"
      to: "Cloudflare Pages"
      via: "deployment configuration"
      pattern: "name.*=.*arthur"
    - from: "dist/_redirects"
      to: "Cloudflare edge"
      via: "redirect rules"
      pattern: "/post/.*"
---

<objective>
Deploy portfolio site to Cloudflare Pages at arthurpfz.com with HTTPS enabled.

Purpose: Meet DSGN-04 requirement for production deployment, make site publicly accessible with SSL/HTTPS.

Output: Live site at arthurpfz.com with SSL certificate, all pages accessible, redirects working.
</objective>

<execution_context>
@/Users/arthurpfalzgraf/.claude/get-shit-done/workflows/execute-plan.md
@/Users/arthurpfalzgraf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/arthurpfalzgraf/Desktop/Projects/PortfolioWebsite/.planning/PROJECT.md
@/Users/arthurpfalzgraf/Desktop/Projects/PortfolioWebsite/.planning/ROADMAP.md
@/Users/arthurpfalzgraf/Desktop/Projects/PortfolioWebsite/.planning/STATE.md
@/Users/arthurpfalzgraf/Desktop/Projects/PortfolioWebsite/public/_redirects
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Wrangler CLI and configure Cloudflare Pages</name>
  <files>package.json, wrangler.toml</files>
  <action>
Install Wrangler CLI for Cloudflare Pages deployment:

1. Add wrangler to devDependencies: `npm install --save-dev wrangler`
2. Create wrangler.toml configuration:
```toml
name = "arthurpfz-portfolio"
pages_build_output_dir = "dist"
compatibility_date = "2024-01-01"
```

3. Add deploy script to package.json:
```json
"scripts": {
  "deploy": "astro build && wrangler pages deploy dist --project-name=arthurpfz-portfolio"
}
```

Project name should match wrangler.toml. Build is included in deploy script to ensure fresh build before deployment.</action>
  <verify>
    <automated>test -f wrangler.toml && grep -q "wrangler pages deploy" package.json</automated>
  </verify>
  <done>wrangler.toml exists and package.json has deploy script with wrangler pages deploy command</done>
</task>

<task type="auto">
  <name>Task 2: Authenticate Wrangler and deploy to Cloudflare Pages</name>
  <files>none (deployment operation)</files>
  <action>
Authenticate Wrangler and deploy site:

1. Run `npx wrangler login` - this will open browser for Cloudflare authentication
2. Once authenticated, run `npm run deploy`
3. Wrangler will create Pages project if it doesn't exist
4. Deployment uploads dist/ contents to Cloudflare edge network

If authentication fails (requires user interaction), create checkpoint for user to complete auth, then Claude retries deploy.

Expected output: Deployment URL (e.g., https://arthurpfz-portfolio.pages.dev)

Note deployment URL from output - this is the preview URL before custom domain is configured.</action>
  <verify>
    <automated>MISSING - Requires manual verification that deployment succeeds and returns URL</automated>
    <manual>Check command output for "Deployment complete" message and *.pages.dev URL</manual>
    <sampling_rate>run after this task commits, before next task begins</sampling_rate>
  </verify>
  <done>Wrangler deploy command succeeds and returns Cloudflare Pages URL</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Deployed portfolio site to Cloudflare Pages with:
- Astro static site build (dist/ folder)
- Wrangler CLI deployment
- Cloudflare edge hosting
- Automatic HTTPS on *.pages.dev domain
  </what-built>
  <how-to-verify>
**Test deployed site:**

1. Visit deployment URL from Task 2 output (https://arthurpfz-portfolio.pages.dev or similar)
2. Verify all pages load:
   - Homepage redirects to /now ✓
   - /archive page shows article list ✓
   - /experience page shows work history ✓
   - /now page renders correctly ✓
   - Individual article pages (/archive/hooked) work ✓
3. Test theme toggle - click sun/moon button, verify colors change ✓
4. Test redirects - visit /post/hooked, should redirect to /archive/hooked ✓
5. Check HTTPS - URL should use https:// with valid certificate ✓
6. Test on mobile - responsive design works ✓

**Expected results:**
- All pages accessible and styled correctly
- Theme toggle works (colors change, persists on refresh)
- Redirects from /post/* to /archive/* work
- HTTPS enabled (green lock icon in browser)
- No console errors

**If issues found:**
Describe specific problems (broken links, styling issues, 404s, etc.)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe issues to fix</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action>Configure custom domain arthurpfz.com in Cloudflare Pages dashboard</action>
  <instructions>
**Configure custom domain:**

1. Visit Cloudflare dashboard: https://dash.cloudflare.com
2. Navigate to Workers & Pages > arthurpfz-portfolio
3. Go to Custom domains tab
4. Click "Set up a custom domain"
5. Enter domain: arthurpfz.com
6. Follow Cloudflare instructions to configure DNS:
   - If domain is already on Cloudflare: DNS records auto-configured
   - If domain is external: Add CNAME record pointing to *.pages.dev

7. Wait for DNS propagation (usually 1-5 minutes, max 24 hours)
8. Verify HTTPS certificate is issued (automatic via Cloudflare)

**Expected outcome:**
- arthurpfz.com points to Cloudflare Pages deployment
- HTTPS/SSL certificate active (green lock icon)
- Old *.pages.dev URL still works but custom domain is primary

**Verification:**
Visit https://arthurpfz.com and confirm site loads with valid SSL certificate
  </instructions>
  <resume-signal>Type "completed" once domain is configured and arthurpfz.com loads with HTTPS</resume-signal>
</task>

</tasks>

<verification>
**Final production checks:**
1. Visit https://arthurpfz.com
2. Verify SSL certificate is valid (click lock icon in browser)
3. Test all pages via production domain
4. Test old Hugo URLs redirect correctly (/post/hooked → /archive/hooked)
5. Verify theme toggle persists across page navigation
6. Check mobile responsiveness on actual device
7. Run Lighthouse audit on production URL (compare to preview scores)

**Success indicators:**
- Site loads in under 3 seconds
- All pages render correctly
- No 404 errors
- HTTPS enabled and valid
- Theme toggle works
- Redirects functional
</verification>

<success_criteria>
- Site is live at arthurpfz.com with valid SSL/HTTPS
- All pages are accessible and render correctly
- Theme toggle functionality works in production
- Old Hugo article URLs redirect to new Astro URLs
- Lighthouse Performance score maintained (90+) on production
- Custom domain DNS configured correctly
- No console errors or broken assets
</success_criteria>

<output>
After completion, create `.planning/phases/05-performance-launch/05-03-SUMMARY.md`
</output>
